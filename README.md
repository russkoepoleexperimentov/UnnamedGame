# UnnamedGame — FPS MVP

Шутер от первого лица на C# без игрового движка: Win32-окно, рендер на Direct3D 11
(через Vortice), физика на BepuPhysics v2.

## Запуск

```
dotnet build UnnamedGame.sln -c Release       # игра и редактор
dotnet run --project UnnamedGame.csproj -c Release
dotnet run --project editor -c Release        # редактор уровней
dotnet run --project editor -c Release -- assets/maps/other.ugmap   # открыть другую карту
dotnet run --project UnnamedGame.csproj -c Release -- assets/maps/other.ugmap   # сыграть её
```

Требуется .NET 9 SDK и Windows с поддержкой D3D11 (при отсутствии GPU-устройства
рендерер откатывается на WARP).

## Управление

| Клавиша | Действие |
|---|---|
| WASD | движение, за рулём — газ/тормоз и руль |
| Space | прыжок (удерживать — bunny-hop), за рулём — ручник |
| Ctrl | присесть; в прыжке подтягивает ноги (crouch-jump), в noclip — снижение |
| Мышь | обзор |
| ЛКМ / F | выстрел физическим шаром |
| E | сесть в машину / выйти из неё (нужно стоять рядом) |
| L | фонарик пешком, фары за рулём |
| R | респавн |
| ~ | консоль (`help` — список команд) |
| R / F1 / F2 / N / B | привязки по умолчанию из `config.cfg`: респавн, оверлей вкл/выкл, noclip, 5 шаров |
| Esc | закрыть консоль; иначе отпустить/захватить курсор |

## Структура

```
src/Assets/FbxParser.cs    бинарный FBX: дерево записей, свойства, deflate-массивы
src/Assets/ModelLoader.cs  сцена FBX -> меши по материалам, узловые трансформы, текстуры
src/Assets/AssetPaths.cs   поиск папки assets рядом с исполняемым файлом
src/Audio/SoundClip.cs     декодирование WAV/OGG/MP3 в 16-битный PCM
src/Audio/AudioEngine.cs   XAudio2: мастер-голос, пул голосов, петли, панорама
src/Audio/GameAudio.cs     логика звука: шаги по поверхностям, слои двигателя
src/UI/FontAtlas.cs        атлас моноширинного шрифта, запекается через GDI+
src/UI/UiBatch.cs          сбор 2D-геометрии кадра в один буфер вершин
src/UI/DevConsole.cs       консоль: history, автодополнение, cvar-ы, команды, бинды, конфиг
src/UI/KeyNames.cs         имена клавиш для bind
src/Platform/Win32.cs      P/Invoke: окно, сообщения, raw input
src/Platform/GameWindow.cs окно, очередь сообщений, клавиатура, относительная мышь
src/Graphics/Shaders.cs    HLSL: G-buffer, карты теней, проход освещения, unlit-оверлей
src/Graphics/Mesh.cs       генерация куба и UV-сферы
src/Graphics/Lights.cs     типы источников света и команда отрисовки
src/Graphics/Texture.cs    загрузка PNG/JPG через GDI+, мип-цепочка на GPU
src/Graphics/RenderModel.cs модель на GPU: меши по материалам, общие текстуры
src/Graphics/Renderer.cs   устройство, swap chain, G-buffer, карты теней, проходы кадра
src/Sim/PhysicsWorld.cs    Simulation, коллбеки narrow phase / pose integrator
src/Sim/Character.cs       движение в стиле Quake/Source на капсуле, шагание по ступенькам
src/Sim/Vehicle.cs         машина: raycast-подвеска, круг трения, стабилизатор
src/Sim/ViewEffects.cs     покачивание камеры при ходьбе и инерция головы в машине
src/Sim/MapData.cs         формат карты (.ugmap): браши, ящики, лампы, точки спавна
editor/src/EditorApp.cs    редактор: панели, выделение, инструменты, undo
editor/src/EditorRenderer.cs  свой мини-рендер: линии, залитые боксы, текст
editor/src/Viewport.cs     орто- и перспективная панели, экран <-> мир
editor/src/EditorUi.cs     виджеты: кнопки, строки списка, поля ввода чисел
src/Sim/Level.cs           блокаут карты: двор, рампы, лестницы, мостки, ящики
src/Game.cs                игровой цикл: фиксированный шаг 120 Гц + интерполяция поз
```

## Как это устроено

**Рендер — deferred.** Кадр состоит из четырёх проходов:

1. **Карта теней солнца** — ортографическая проекция 2048², охватывает 64×64 м
   вокруг игрока; начало координат привязывается к целым тексёлям, иначе тени
   «ползут» при ходьбе.
2. **Карта теней фонарика** — перспективная 1024² по конусу прожектора, строится
   только когда фонарик включён.
3. **G-buffer** — альбедо (RGBA8) и мировая нормаль (R10G10B10A2) плюс глубина
   (R32_Typeless, читается как SRV в проходе освещения).
4. **Освещение** — один полноэкранный треугольник (без вершинного буфера, из
   `SV_VertexID`): мировая позиция восстанавливается из глубины через обратную
   view-projection, затем накапливаются солнце, фонарик и до 16 точечных
   источников. Тень берётся 3×3 PCF через `SamplerComparisonState`.

5. **Стёкла** — forward-проход с альфа-блендингом поверх собранного кадра.

Поверх идёт unlit-проход для прицела — иначе он попал бы в G-buffer и его бы
«осветило». Клетчатая «текстура» считается процедурно из мировых координат по
доминирующей оси нормали, поэтому ассеты не нужны.

**Стёкла (forward-проход).** В deferred прозрачности нет: G-buffer хранит одну
поверхность на пиксель. Поэтому стеклянные материалы не попадают в геометрический
проход, а рисуются после освещения отдельным проходом с блендингом
`SrcAlpha / InvSrcAlpha`. Детали, из-за которых это работает:

- глубина **тестируется, но не пишется** — иначе стёкла перекрывали бы друг друга в
  случайном порядке отрисовки;
- отсечение граней выключено: окно видно и снаружи, и из салона, а нормаль в
  пиксельном шейдере разворачивается к камере;
- команды сортируются по расстоянию от камеры от дальних к ближним;
- прозрачность берётся из FBX: `Opacity` материала (у рассеивателей фар 0.058) и
  альфа-канал карты прозрачности (у лобового 0.09) — стекло почти чистое;
- поверх идёт френель по Шлику: в лоб стекло прозрачное, на скользящем угле —
  почти зеркало. Отсюда же отражение неба и блик солнца.

Стёкла не пишутся в карты теней и не участвуют в G-buffer, поэтому теней не
отбрасывают.

**Динамический свет.** Четыре точечных источника (`Level.UpdateLights`) каждый
кадр пересчитывают позицию и яркость: два вращаются по орбите, один мерцает.
Затухание — оконная обратно-квадратичная функция, поэтому источник гарантированно
гаснет на границе своего радиуса и не требует отсечения. Теней они не отбрасывают:
для этого нужна кубическая карта на каждый источник.

**Фонарик (L).** Прожектор в позиции камеры со сдвигом вниз-вправо, конус со
сглаженными краями (`smoothstep` между косинусами внутреннего и внешнего угла) и
собственной картой теней, поэтому луч корректно перекрывается ящиками и стенами.

**Тени.** Смещение задаётся тремя независимыми способами: отсечение передних
граней в проходе тени, slope-scaled depth bias в растеризаторе и сдвиг точки
выборки вдоль нормали. Вместе они убирают и acne, и большую часть peter-panning.
Освещение сумеречное намеренно: при ярком солнце динамический свет и фонарик
теряются на фоне рассеянного света.

**Физика.** `Simulation` c гравитацией -18 м/с² (быстрее реальной — как в шутерах
той эпохи), демпфирование в `IntegrateVelocity`, один материал трения на все пары.
Статика карты — боксы, динамика — ящики и выстреливаемые шары.

**Движение.** Персонаж — динамическая капсула с обнулённым обратным тензором
инерции (не заваливается), скорость которой задаётся каждый шаг вручную, а стены
разрешает солвер. Формулы взяты из Quake/Source: `Accelerate` добавляет скорость
только до проекции на wish-направление, на земле работает экспоненциальное трение,
в воздухе wish-скорость обрезается до `AirSpeedCap` — отсюда и air-strafe, и
bunny-hop. Земля определяется лучом вниз из центра капсулы с проверкой наклона
нормали (≤ 45°). Есть coyote-time и буфер прыжка по 0.1 с.

**Приседание и crouch-jump.** Капсула переключается между двумя заранее созданными
формами: 2.0 м стоя и 1.1 м сидя (`Bodies.SetShape`). Куда именно она сжимается,
зависит от того, где игрок:

- **на земле** ступни остаются на месте, а тело сжимается вниз;
- **в воздухе** наоборот: на месте остаётся голова, а ступни подтягиваются вверх
  на 0.9 м. Это и есть crouch-jump — с ним берутся уступы, недостижимые обычным
  прыжком. Так же устроен `PM_Duck` в Source.

Проверено численно на блоке высотой 1.5 м: обычный прыжок поднимает ступни на
0.74 м и упирается в грань, а с приседом в воздухе игрок встаёт на уступ.
Разгибание работает симметрично: на земле растёт вверх, в воздухе — вниз, и в
обоих случаях сначала проверяется лучом, хватает ли места; если нет, персонаж
остаётся сидеть. Скорость на корточках — 3.2 м/с вместо 8. В карту добавлена
низкая плита с просветом 1.25 м, чтобы было где ползать.

**Камера в машине.** Обзор задаётся не мировыми углами, а системой координат
машины: направление взгляда строится из её `Forward`/`Right`/`Up`, а мышь добавляет
лишь смещение поверх. Поэтому при повороте руля камера едет вместе с кузовом — если
смотреть вперёд, взгляд остаётся вперёд, — а крен и дифферент кузова на подвеске
передаются в кадр. Проверено: без единого движения мышью поворот налево развернул
взгляд со −160° до −15.6°. При выходе из машины направление переводится обратно в
мировые углы, чтобы вид не прыгнул.

Углы обзора в машине живут в той же конвенции, что и у персонажа: yaw растёт влево,
потому что дельта мыши инвертируется один раз, на вводе, и обе камеры обязаны читать
её одинаково. Сама формула вынесена в `ViewEffects.CarLookDirection`, чтобы её можно
было сверить с `Character.Forward` тестом, а не на глаз: при одном и том же движении
мыши обе дают побитово совпадающий вектор.

**Инерция головы в машине.** Ускорение берётся как производная скорости, проецируется
на оси машины и сглаживается за 0.1 с. Голова отстаёт от кузова: при разгоне уезжает
назад и камера чуть задирается, при торможении подаётся вперёд и клюёт носом, в
повороте смещается наружу и накреняется. Замеры на синтетических режимах: резкий
разгон (10 м/с²) — голова назад на 80 мм, тангаж +2.3°; резкое торможение (15 м/с²) —
вперёд на 120 мм, −3.4°; поворот — вбок на 50 мм и крен 2°. На равномерном ходу
ровно ноль. Отключается `cl_carsway 0`.

**Покачивание при ходьбе.** Фаза набегает не по времени, а по пройденному
расстоянию — по тому же шагу 2.1 м, что и звуки шагов, поэтому качание не уплывает
относительно них. Вертикаль идёт как `-|cos(фазы)|`: нижняя точка приходится ровно
на касание ноги, а не на середину шага (в первой версии было наоборот). Боковое
смещение — `sin(фазы)`, то есть один цикл на два шага, с небольшим креном. Амплитуда
пропорциональна скорости: 45 мм на 8 м/с, 22 мм на 4 м/с, 8 мм на 1.5 м/с, и плавно
гаснет при остановке и в воздухе. Приземление сбрасывает фазу в нижнюю точку — читается
как удар. Отключается `cl_bob 0`.

**Сглаживание вида.** Всё, что мгновенно двигает глаз — шаг на ступеньку, притяжка
к земле, присед, вставание — добавляется в один накопитель `_viewOffset`, который
вычитается из позиции камеры и сходит к нулю. То есть камера всегда движется
непрерывно, а тело телепортируется. Ключевая деталь — скорость возврата: раньше
стояло экспоненциальное затухание `pow(0.0001, dt/T)`, которое съедало 47%
смещения за подшаг; для полуметрового шага это было терпимо, а для 0.9 м приседа
читалось как рывок (411 мм за подшаг). Теперь смещение гасится с ограничением
скорости от 1 до 5 м/с, как stair smoothing в Source: худший сдвиг камеры за
подшаг — 43 мм, меньше, чем 65 мм обычной ходьбы.

**Шагание по ступенькам.** Солвер не даёт капсуле забраться на уступ, поэтому
подъём делается вручную тремя лучами: вперёд на уровне голени (есть ли стена),
вниз с высоты `MaxStepHeight` перед игроком (где поверхность сверху) и вверх с
этой поверхности (влезет ли игрок). Если подъём в пределах 0.55 м, капсула
ставится **абсолютно** по найденной поверхности (`surfaceY + полувысота`), а не
сдвигается на дельту — иначе попытки на соседних подшагах складывались бы и
выбрасывали игрока выше ступени. Камера не телепортируется вместе с капсулой:
величина подъёма копится в `_stepOffset` и затухает экспоненциально за ~0.09 с,
как stair smoothing в Source.

**Спуск по склонам.** Два классических приёма из Quake/Source. Первый —
`ClipToGroundPlane` (аналог `PM_ClipVelocity`): на земле скорость не «сплющивается»
в горизонталь, а проецируется на плоскость земли с сохранением модуля, поэтому вниз
по склону игрок едет по склону, а не срывается с него. Второй — ground snapping
(`TryStepDown`, аналог `CategorizePosition` / `StayOnGround`): если в прошлом подшаге
игрок был на земле, не прыгал и не движется вверх, а земля пропала, капсула
притягивается вниз к поверхности в пределах `GroundSnapDistance` (0.6 м — чуть
больше высоты ступеньки). Притяжение так же сглаживается `_stepOffset`, поэтому
камера не дёргается. Снап подавляется на 0.15 с после прыжка и на 0.1 с после шага
вверх — иначе на кромке ступени луч находит нижнюю проступь и стаскивает игрока
обратно с лестницы.

**Интерполяция.** Физика идёт с фиксированным шагом, рендер — с частотой vsync,
поэтому перед каждым шагом поза каждого тела копируется в `PreviousPose`, а при
отрисовке позиция берётся как `Lerp`, ориентация — как `Slerp` от остатка
аккумулятора (`alpha = _accumulator / FixedTimestep`). Камера интерполируется
так же. Без этого при несовпадении 120 Гц и частоты кадров движение дёргалось бы.

**Цикл.** Симуляция идёт фиксированным шагом 1/120 с с аккумулятором (не более
8 шагов за кадр, чтобы не было спирали смерти), рендер — с частотой vsync.

## Машина

Модель — `assets/models/vehicles/lada vaz 2110.fbx` (ВАЗ-2110, 429 тыс. треугольников,
16 объектов, 37 материалов, 11 текстур). Загружается своим парсером, без Assimp и
прочих библиотек импорта.

**Парсер FBX.** Файл бинарный, версия 7400: дерево записей `[конец, число свойств,
длина, имя, свойства, вложенные записи]`, массивы сжаты zlib (`ZLibStream`).
`ModelLoader` разбирает то, что реально использует эта модель: полигональные меши
(веер треугольников), нормали и UV с раскладкой `ByPolygonVertex`, материалы
`ByPolygon`, узловые трансформы и связи `Connections`. Единицы модели —
сантиметры, поэтому все трансформы домножаются на 0.01; получается ровно
1.81 x 1.41 x 4.24 м — габариты настоящей «десятки». Одинаковые углы полигонов
свариваются словарём, иначе вершин было бы втрое больше. Загрузка занимает ~1.3 с.

**Обход вершин.** FBX задаёт полигоны в порядке, противоположном тому, который
используют процедурные меши в `Mesh.cs`, поэтому при развороте в треугольники
последние две вершины меняются местами. Без этого растеризатор отсекал лицевые
грани и показывал изнанку дальнего борта — снаружи машина выглядела чёрной и
просвечивала. Проверяется численно: `cross(b-a, c-a)` должен быть противоположен
нормали вершины, как у геометрии из `Mesh.cs`.

**Материалы.** У 11 материалов есть текстуры, у остальных только `DiffuseColor` из
FBX. Пути в файле ведут на машину художника, поэтому от них берётся только имя
файла и ищется в `assets/textures/vehicles`. Текстуры декодируются через GDI+ и заливаются в
`B8G8R8A8` с мип-цепочкой (`GenerateMips`).

**Физика.** Классическая raycast-машина: одно твёрдое тело-коробка плюс четыре
колеса, которые каждый подшаг простреливаются лучом вниз. Точки крепления подвески
берутся прямо из узлов колёс модели, так что физика совпадает с геометрией
(колёсная база 2.45 м, колея 1.36 м). Пружина с демпфером держит кузов, сцепление
считается через **круг трения**: продольный и боковой импульсы делят общий бюджет
`μ * N * dt`. Первая версия просто гасила снос целиком — машина переворачивалась в
первом же повороте. Дополнительно стоят стабилизатор поперечной устойчивости и
уменьшенное вдвое плечо трения по высоте: крен виден, но кувырка нет.
Разгон 0-83 км/ч за 4 с, задний ход ограничен 29 км/ч.

**Посадка и высадка (E).** Рядом с машиной (до 3.4 м) капсула игрока удаляется из
симуляции, камера садится на место водителя (левый руль), взгляд разворачивается по
курсу машины; управление уходит на колёса. При выходе капсула возвращается со
стороны водителя, а если там стена — с противоположной. Всё от первого лица:
из салона видно руль, панель и капот.

## Редактор уровней

Отдельное приложение `UnnamedGame.Editor` (решение `UnnamedGame.sln`, проект в
`editor/`). Оно ссылается на проект игры и переиспользует его окно, шрифт, формат
карты и HLSL текстового прохода, но имеет собственный рендер: без G-buffer и теней,
только линии, залитые боксы и текст — редактору нужна ясность, а не картинка.

**Карта стала данными.** Уровень больше не зашит в код: `MapData` — это брашей
(коробки с поворотом, цветом, поверхностью и флагом клетки), ящики, лампы и точки
спавна игрока и машины, сериализуемые в JSON `.ugmap`. Прежний двор сохранён как
`Level.CreateDefaultMap()` и служит запасным вариантом: если файла нет, игра
создаёт его при старте. Проверено, что запись и чтение дают побитово тот же
уровень — контрольная сумма всех полей совпала, и маршруты по поверхностям
(бетон, металл, гравий) остались прежними.

**Четыре панели, как в Hammer:** перспектива и три ортогональные — сверху (x/z),
спереди (x/y, взгляд с юга на север, как смотрит игрок на спавне) и сбоку (z/y,
взгляд с запада на восток). Нормаль каждой панели не задаётся вручную, а выводится
как `cross(right, up)`: правостороннее `CreateLookAt` кладёт на экранное «вправо»
именно `cross(up, normal)`, поэтому произвольно выбранная нормаль зеркалит панель
относительно мыши. Такой баг там и был — во front и side. У каждой свои пан и зум (зум идёт к курсору, точка под
ним остаётся на месте), сетка сама огрубляется при отдалении, чтобы не превращаться
в сплошную заливку. В перспективе — свободный полёт на ПКМ + WASD.

**Работа с геометрией.** Протяжка ЛКМ по пустой сетке создаёт браш: две оси берутся
из протяжки, третья получает разумную толщину, которую потом правят в соседней
панели. Клик выделяет (из перекрывающихся выбирается самый маленький, иначе до
мелкого блока на большом полу не добраться), протяжка внутри двигает, протяжка за
одну из восьми ручек тянет соответствующую грань.

**Поворот мышью.** `R` переключает те же восемь ручек между растяжением и поворотом,
как в Hammer: в режиме поворота они рисуются ромбами и обводятся кольцом, а протяжка
за любую из них крутит объект вокруг нормали текущей панели — то есть вокруг той оси,
которую вы визуально вращаете. При включённой привязке угол ложится на 15°, текущее
значение видно в статус-баре. Поворачиваются браши (в любой из трёх панелей) и точка
спавна машины (только сверху — машина крутится вокруг вертикали).

Формат карты хранит углы Эйлера, а не кватернион, чтобы файл оставался читаемым,
поэтому после поворота ориентация раскладывается обратно в углы. Разложение сверено
численно: на 20000 случайных вращений максимальная ошибка оси 1.7e-5, а углы уже
имеющихся в карте рамп (−21.77° и 33.21°) проходят цикл кватернион → углы →
кватернион без изменений. В 3D выделение идёт лучом с
проверкой по граням в системе координат самого браша, поэтому повёрнутые рампы
выделяются корректно. Есть привязка к сетке (0.125–4 м), удаление, дублирование и
undo на 32 шага.

**Панель инструментов.** Сверху — кнопки файлов (new/open/save), выбор инструмента
(select, brush, terrain, light, crate), переключатель ручек resize/rotate, привязка,
шаг сетки и показ подсказок. Инструмент определяет, что делает протяжка ЛКМ в панели:
select двигает и растягивает, brush создаёт коробки, terrain лепит рельеф, light и
crate ставят объект по клику.

**Список объектов.** Справа сверху — все объекты карты (рельеф, браши, ящики, лампы,
точки спавна) с краткими характеристиками. Клик выделяет объект, колесо прокручивает.

**Инспектор.** Справа снизу — свойства выделенного: позиция, размер и поворот покомпонентно,
масса и размер ящика, цвет/радиус/яркость/орбита/скорость/мерцание лампы, курс машины.
Числовые поля редактируются кликом и вводом с клавиатуры (Enter применяет, Escape
отменяет); пока поле в фокусе, горячие клавиши редактора не срабатывают. Кнопками
переключаются поверхность, клетка и цвет.

**Рельеф (terrain).** Новый тип объекта в карте: регулярная сетка высот с шагом ячейки,
поверхностью, наземным материалом и цветом. В редакторе патч создаётся протяжкой в виде сверху, а лепится
удержанием ЛКМ: мягкая радиальная кисть поднимает грунт, с Shift — опускает; радиус и
сила задаются в инспекторе, есть кнопка выравнивания. В игре рельеф превращается в
статический треугольный меш Bepu и обычный меш для рендера с нормалями из поля высот,
поэтому по нему ходят, ездят и слышат шаги его поверхности — наконец пригодились банки
earth и grass.

**Наземные материалы.** Патч ссылается на текстуру из `assets/textures/ground` по имени
(`gravel`, `mud`, `sand`, `stoned_dirt`, `stoned_grass`); рядом лежит карта нормалей
`<имя>_normal.png`, она подхватывается автоматически. Тайлинг считается от мировых
координат (`world.xz / tile size`), а не от UV сетки, поэтому размер плитки не зависит
от того, насколько мелко разбит рельеф, и не меняется при его пересборке. Карта нормалей
применяется без тангентов в вершинах: у поля высот касательный базис — это мировые оси,
спроецированные на поверхность, и он строится прямо в пиксельном шейдере. Материал и
размер плитки переключаются в инспекторе редактора; в его 3D-панели рельеф остаётся
залитым цветом — редактору важна форма, а не отделка.

Цвет у текстурированного патча игнорируется (берётся белый): иначе старая карта, где
цвет был зелёным, перекрасила бы любую текстуру. Цвет работает только когда текстуры нет.

Тонкость, стоившая отладки: треугольники меша в Bepu **односторонние**, а нормаль
считается как `cross(C - A, B - A)`. При обратной намотке рельеф выглядит правильно во
всех видах, но проваливается насквозь: игрок пролетает сквозь него, лучи не находят
опоры. Проверяется падающим шаром — он должен остановиться на высоте поверхности.

**Файлы.** `Ctrl+N` — новая карта (пустой пол 32x32 и обе точки спавна, чтобы по ней
сразу можно было ходить), `Ctrl+O` — открыть, `Ctrl+S` — сохранить, `Ctrl+Shift+S` —
сохранить как. Диалоги настоящие, системные: `GetOpenFileNameW`/`GetSaveFileNameW` из
comdlg32 и `MessageBoxW` — своя тонкая обёртка в `src/Platform/Dialogs.cs`, доступная
и игре. Заголовок окна и статус-бар показывают имя файла и звёздочку, если есть
несохранённые правки; при создании, открытии и закрытии окна такие правки не теряются
молча — сначала спрашивают. Путь к карте можно передать и аргументом командной строки.

Точка входа редактора помечена `[STAThread]`, и это не формальность: shell-диалоги
шлют сообщения окну-владельцу, поэтому запуск их на рабочем потоке, пока главный
поток ждёт в `Join`, приводит к дедлоку — диалог не появляется вовсе, а редактор
намертво зависает. Именно так и вышло с первой версии; теперь диалог открывается в
том же потоке, а обёртка на не-STA вызове честно отбрасывает владельца.

**Прочее содержимое:** лампы (`L`) с показанным радиусом, ящики (`B`), точки спавна
игрока и машины (`1`/`2`) — всё выделяется и двигается так же. `M` перебирает
поверхность браша (она же определяет звук шагов), `K` — клетку, `C` — цвет,
`F` наводит все панели на выделенное, `Ctrl+S` сохраняет. `F1` прячет список клавиш.

Проверка сквозная: скриптом послал редактору движения мыши и нажатия, он создал
браш 15.5 x 16 м, показал его во всех четырёх панелях с ручками, сохранил файл
(28 брашей вместо 27), после чего игра загрузила изменённую карту и запустилась
штатно.

## Звук

Воспроизведение — **XAudio2** (`Vortice.XAudio2`), в тон остальному проекту: своя
обёртка с мастер-голосом, пулом источников для одиночных звуков и петлями для
двигателя. Позиционирование считается вручную — затухание `1/(1+d²/r²)` и
панорама постоянной мощности через `SetOutputMatrix`; полноценный X3DAudio для
такой сцены избыточен. Декодирование: WAV разбирается своим RIFF-парсером,
OGG Vorbis — через NVorbis, MP3 — через NLayer (это кодеки, не движки; писать
декодер Vorbis с нуля вне рамок задачи). Все 34 файла грузятся целиком в память,
это ~2 МБ PCM.

**Шаги** ставятся по пройденному расстоянию, а не по таймеру: каждые 2.1 м
проигрывается случайный сэмпл из банка текущей поверхности со случайной высотой
тона (±7%). Поэтому темп шагов сам совпадает с реальной скоростью, а на бегу
получается около четырёх шагов в секунду. Отдельно звучит приземление — оно же
играет, когда игрок выходит из машины.

**Поверхности** определяются лучом земли: `Character` запоминает, на какой
коллайдер он опирается, а `Level` знает, из чего сделан каждый статический объект.
В блокауте это бетон (двор, стены, укрытия), металл (мостки, опоры, обе рампы,
лестницы) и гравий (крыша-платформа). Банки earth и grass загружаются, но в этой
карте поверхностей для них нет.

**Двигатель** собран из четырёх петель: `AccelerationLow/High` и
`DecelerationLow/High`. Все четыре звучат одновременно с разными громкостями —
низкие и высокие обороты кроссфейдятся по скорости, а наборы «газ» и «накат» — по
положению педали со сглаживанием 0.12 с. Высота тона тянется от 0.85 до 1.85.
Так переходы не щёлкают. Пятая петля — `Skid` — включается от бокового
проскальзывания шин (`Vehicle.Slip`), то есть на ручнике и в заносе. Двигатель
работает только пока игрок за рулём.

## Консоль и отладочный оверлей

Текста в проекте до этого не было, поэтому сначала появился **атлас шрифта**: при
старте GDI+ рисует ASCII 32..126 в сетку ячеек (Consolas 15 px) и заливает её в
текстуру. Шрифт моноширинный, так что UV символа считаются прямо из его кода —
таблица метрик не нужна. Нулевая ячейка оставлена белой: ей же рисуются панели и
линии, поэтому весь 2D идёт одной текстурой и одним вызовом отрисовки.

Весь интерфейс собирается в `UiBatch` в пикселях (начало координат — левый верхний
угол) и рисуется отдельным проходом после стекла: динамический вершинный буфер,
альфа-блендинг, без глубины. Прицел переехал туда же из 3D — раньше он был двумя
маленькими кубиками перед камерой.

**Консоль (`~`)** сделана по образцу Quake: выезжает сверху на 45% экрана,
хранит 512 строк истории вывода, свой список введённых команд (Up/Down),
автодополнение по Tab (дополняет до общего префикса и печатает варианты),
прокрутку PgUp/PgDn и мигающий курсор. Пока она открыта, игра не получает ввод:
ни движения, ни обзора, ни выстрелов. Escape закрывает.

Ввод текста берётся из `WM_CHAR`, а не из `WM_KEYDOWN`: так работают и раскладка,
и автоповтор при удержании клавиши, и backspace с Enter приходят тем же путём.
Клавиша `~` (она же `ё`) фильтруется, чтобы не попадать в строку ввода.

**Команды и переменные** регистрируются снаружи, консоль не знает об игре:
`help`, `clear`, `echo`, `cvarlist`, `quit`, `pos`, `teleport x y z`, `respawn`,
`spawn n`, `car`. Переменные: `player_debug`, `snd_volume`, `timescale`, `cl_bob`,
`cl_carsway`. Значение
печатается по имени переменной, задаётся `имя значение`.

**Привязки клавиш.** `bind <клавиша> <команда...>` вешает любую команду на клавишу:
`bind f1 player_debug 1`, `bind n noclip`, `bind b spawn 5`. Есть `unbind`,
`unbindall` и `binds` для списка. Имена клавиш — в `KeyNames`: буквы, цифры,
f1-f12, space, ctrl, стрелки, pgup и прочее. Привязки срабатывают по нажатию
(в кадре, когда клавиша пошла вниз), и только когда консоль закрыта. Удержания
в стиле `+forward` нет — это команды-импульсы.

**Конфиг.** `config.cfg` лежит рядом с папкой `assets`, чтобы переживать
пересборку `bin/`. При старте он исполняется через `exec`; если файла нет, он
создаётся с настройками и привязками по умолчанию. Формат — обычные строки
консоли, `//` начинает комментарий. При выходе (командой `quit` или закрытием
окна) файл перезаписывается текущими значениями переменных и привязками, так что
`player_debug 1`, выставленный в игре, переживёт перезапуск. Своей рукой
дописанные комментарии при этом теряются — как в оригинальных играх. Команда
`saveconfig` пишет файл вручную, `exec <файл>` исполняет любой другой.

**`noclip`** переключает свободный полёт: капсула убирается из симуляции,
движение идёт по направлению взгляда, Space/Ctrl — вверх и вниз, Shift — ускорение
втрое. При выключении капсула возвращается в мир в той же точке. Сесть в машину
из noclip нельзя — режим сначала выключается.

**`player_debug 1`** включает оверлей в левом верхнем углу: позиция, вектор
скорости, скорость (горизонтальная, полная, км/ч), направление взгляда (yaw/pitch
и вектор), состояние (`on foot (ground)`, `on foot (air)`, `driving (4/4 wheels
down)`), поверхность под ногами, нормаль земли, fps и число тел. Под открытой
консолью оверлей прячется, чтобы не накладываться на её текст.

## Что не сделано (осознанно, это MVP)

- при подъёме по лестнице персонаж на пару подшагов из тринадцати считается в
  воздухе (солвер выталкивает капсулу из подступёнка) — на плавность это не
  влияет, но немного снижает разгон; лечится sweep-тестом капсулы вместо лучей;
- стёкла не отбрасывают теней и не преломляют изображение за собой;
- сортировка прозрачного идёт по объектам, а не по треугольникам: у пересекающихся
  стёкол одного меша порядок может быть неверным;
- кузов односторонний: изнутри его внутренние поверхности освещены только
  рассеянным светом, потому что нормали смотрят наружу;
- карты нормалей из assets не используются: в вершинах нет тангентов;
- точечные источники не отбрасывают теней (нужны кубические карты);
- одна каскадная карта теней на солнце — на больших расстояниях тени грубоваты;
- шрифт консоли только ASCII: кириллица в выводе превратится в «?»;
- привязки только на нажатие: аналога `+forward`/`-forward` нет;
- присед в воздухе не проверяет, во что упрутся поднятые ноги: решает солвер;
- `noclip` не сохраняется в конфиг (это состояние, а не переменная);
- рельеф лепится только в виде сверху и только высотами: ни отверстий, ни смешивания
  двух материалов на одном патче;
- `assets/textures/gfx/clouds.png` не используется: небо пока залито одним цветом;
- у брашей по-прежнему только форма коробки;
- нет списка недавних файлов и вкладок: одновременно открыта одна карта;
- нет текстур на блокауте, скайбокса и оружия в руках;
- звуки моно и без реверберации: XAudio2 умеет и то и другое, но это уже тюнинг;
- у ящиков и шаров нет звуков столкновений — озвучены только шаги и машина;
- прицел рисуется двумя маленькими боксами перед камерой, а не 2D-проходом.
